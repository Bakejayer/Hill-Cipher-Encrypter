/*package logic;

import java.util.Scanner;

public class MatrixDriver {

	public static void main(String[] args) {
		
		Matrix messageMat;
		Scanner scnr = new Scanner(System.in);
		String message = "My name is Jacob";
		String encryptedMessage, decryptedMessage;
		
		encryptedMessage = CypherOps.encryptMessage(message);
		System.out.println("\"" + message + "\" encrypted is \"" + encryptedMessage + "\"");
		
		decryptedMessage = CypherOps.decryptMessage(encryptedMessage);
		System.out.println("\"" + encryptedMessage + "\" decrypted is \"" + decryptedMessage + "\"");
	}

}*/
